{{!-- Flash-message partial. Placeholder; Task 2 of Plan 8a replaces this with the {{#if flash}}<div class="flash flash-{{flash.kind}}">{{flash.message}}</div>{{/if}} fragment. Empty stub so @fastify/view's eager partial-load on first render does not 500 on a missing file. --}}
