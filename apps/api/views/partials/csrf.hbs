{{!-- CSRF hidden-input partial. Placeholder; Task 2 of Plan 8a replaces this with <input type="hidden" name="_csrf" value="{{csrfToken}}"> once the csrf-helper decorator lands. Empty stub so Task 1 view-plugin tests can render home-placeholder.hbs (which references {{> csrf}}) without a missing-partial 500. --}}
